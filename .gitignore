.DS_Store
*.log

# Duplicate of assets/design/project/ — old Claude Design zip extract at repo root
# extracted both a flattened and a nested copy. The bundle copy is the one kept.
/project/

# Admin / env
admin/node_modules/
admin/dist/
admin/.env
.env
.env.local
